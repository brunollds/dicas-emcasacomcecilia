name: Atualizar Preços

on:
  # Agendado: 09:00 e 18:00 horário de Brasília (UTC-3)
  schedule:
    - cron: '0 12 * * *'  # 09:00 BRT
    - cron: '0 21 * * *'  # 18:00 BRT
  
  # Permite execução manual
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Instalar dependências
        run: |
          pip install requests
      
      - name: Executar scraper
        run: |
          cd scripts
          python -c "
from price_scraper import atualizar_products_json
atualizar_products_json('../public/data/products.json')
"
      
      - name: Commit e push se houver mudanças
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add public/data/products.json
          
          if git diff --staged --quiet; then
            echo "Nenhuma alteração nos preços"
          else
            git commit -m "🔄 Atualização automática de preços - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi
